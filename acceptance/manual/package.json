{
  "name": "jasper-living-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Living-documentation generator: a Cucumber JSON test feed becomes ONE self-contained HTML manual — the report organized by feature, one collapsible line per step, screenshots linked from disk. The engine (src/engine) is generic; only annotations.ts and the suite's .chapters file are product-specific.",
  "scripts": {
    "manual": "tsx src/engine/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  }
}
